<?xml version="1.0"?>
<!--
 * @version $Id$
 -->
<root>
    <SRS><![CDATA[
        ActorUser is a "human being with identity".
        ActorUser is an ActorVisitor.
        UC1 where ActorVisitor (the visitor) becomes ActorUser (the user):
         1. The visitor updates credentials of the user.
         2. The visitor turns into the user.
        ActorVisitor is a "human being or a computer, without identity".
        ActorVisitor includes:
         IP: "IP address".
        ActorUser includes:
         credentials;
         name: text.
        Credentials of ActorUser include: login and password.
        ]]></SRS>
    <invariants>
        <!-- The XML contains this root element -->
        <invariant>/rqdql</invariant>
        <!-- There is a report present in the XML -->
        <invariant>/rqdql/errors/report[@max='5']</invariant>
        <!-- The report is empty -->
        <invariant>/rqdql/errors/report[not(exists(error))]</invariant>
        <!-- The report is present -->
        <invariant>/rqdql/metrics/report</invariant>
        <!-- The report is present -->
        <invariant>/rqdql/links/report</invariant>
        <!-- The report is present -->
        <invariant>/rqdql/uml/report</invariant>
        <!-- The report is present -->
        <invariant>/rqdql/tc/report</invariant>
    </invariants>
</root>
