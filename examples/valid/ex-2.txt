#ActorUser: a human being with identity.

#ActorVisitor: originator of HTTP request.
#ActorUser is a: ActorVisitor.
#ActorManager is a: ActorUser.
#ActorUser includes: name; email: string as of RFC-s; password.
#ActorVisitor produces: visits: HTTP requests; visits produce: IP address; time.
#
#
R1: ActorUser can manage files of ContentPage.
R4: ActorUser can print, list, and create ContentPage.
    R1.1: ActorVisitor can list, read, print ContentPage
        (only public properties of them;
        if visibility of them allow).
    R1.2: ActorVisitor can create application of ActorUser;
        ActorManager can create (using application of it) ActorUser;
        ActorUser can reset password of himself;
        ActorManager can reset password of ActorUser.
    R1.3: ActorInvestor can RL+P InvestmentProject.
    R1.4: ActorManager can CRUL+P ContentPage.
    R1.5: ActorManager can create, update FieldTrip.
    R1.6: ActorManager can CRUL+P InvestmentProject.
    R1.7: ActorManager can list, approve applications of ActorUser.
    