ActorUser is a "human".
PhotoFile is a "file".
UC1 where ActorUser (the user) uploads PhotoFile (the file):
1. "The user uploads the file".
2. The user reads the file.
3. We validate the file.
UC2 where we validate PhotoFile (the file): "we will define it later".