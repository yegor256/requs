<sample>
    <spec>
        UC1 where User (a user) works:
        1. The user creates;
        3. The user reads;
        2. The user lists.
    </spec>
    <xpaths>
        <xpath>/processing-instruction()[contains(.,'href')]</xpath>
        <xpath>/spec/errors[count(error)=2]</xpath>
    </xpaths>
</sample>
